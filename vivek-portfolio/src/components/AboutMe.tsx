import React from 'react';

// This component is not used since About Me section is included in Header
// Keeping it for potential future use
const AboutMe: React.FC = () => {
  return (
    <section className="about-me">
      {/* About Me content is included in Header component */}
    </section>
  );
};

export default AboutMe;